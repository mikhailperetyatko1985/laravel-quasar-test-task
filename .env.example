# Переменные окружения для Docker Compose
# Скопируйте этот файл в .env и укажите свои значения

# SSL Configuration для продакшена
# Email для уведомлений Let's Encrypt (ОБЯЗАТЕЛЬНО для продакшена!)
LETSENCRYPT_EMAIL=your-email@example.com

# SSL Mode: ip, domain, или self-signed
SSL_MODE=self-signed

# IP адрес сервера (для продакшена с SSL_MODE=ip)
SERVER_IP=

# Домен сервера (для продакшена с SSL_MODE=domain)
SERVER_DOMAIN=
