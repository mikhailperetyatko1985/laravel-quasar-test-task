# Переменные окружения для продакшен среды
# Скопируйте этот файл в .env перед запуском:
# cp .env.prod.example .env

# SSL Configuration
# Email для уведомлений Let's Encrypt (ОБЯЗАТЕЛЬНО для продакшена!)
LETSENCRYPT_EMAIL=your-email@example.com

# SSL Mode: ip (Let's Encrypt с IP), domain (Let's Encrypt с доменом), self-signed (самоподписанный)
SSL_MODE=ip

# IP адрес вашего продакшен сервера (обязательно укажите свой IP!)
# Пример реального IP:
SERVER_IP=78.140.243.138

# Домен сервера (если есть, опционально)
SERVER_DOMAIN=
